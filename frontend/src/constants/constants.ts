export enum BasicUrl {
  BASE_URL = "http://localhost:5000/api/",
}

export enum Methods {
  POST = "post",
  PUT = "put",
  PATCH = "patch",
  GET = "get",
  DELETE = "delete",
}

export enum AuthUrl {
  LOGIN_API_URL = "/users/login",
  REGISTER_API_URL = "/users/register",
}
